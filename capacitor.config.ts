import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.haeldo.geradormega',
  appName: 'Gerador Mega Inteligente',
  webDir: 'dist'
};

export default config;
